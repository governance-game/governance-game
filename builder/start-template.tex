
\documentclass{article}
\usepackage[
	paperwidth=9.4cm,
	paperheight=6.3cm,
	headheight=0cm,
	margin=0.5cm
]{geometry}
\usepackage[utf8]{inputenc}
\usepackage{etoolbox,lineno}% http://ctan.org/pkg/{etoolbox,lineno}
\usepackage{ifthen} % Used to place text only if there is some on the card
\usepackage{fancybox} % Used to make ovalbox

%\usepackage{showframe}
%\renewcommand*\ShowFrameColor{\color{red}}

\input{template-font}
\setlength\parindent{0pt} % we do not want to repeat \noindent everwhere
\begin{document}
\pagenumbering{gobble} % turn off pagenumbers
\hyphenchar\font=-1 % turn off end-of-line word hyphenation
\centerline{\textbf{Starting state}}
\ovalbox{
\noindent\begin{minipage}[c][4cm][c]{\textwidth} % A minipage is used to get a fixed size for the table
\centering
% Use a 3x3 table to place the values, but only if there are values given on the card
\begin{tabular}{p{0.25\linewidth} p{0.25\linewidth} p{0.25\linewidth}}
\ifthenelse{\isundefined{\upperleft}}{}{\upperleft} &
\ifthenelse{\isundefined{\uppercenter}}{}{\centering\uppercenter} &
\ifthenelse{\isundefined{\upperright}}{}{\hfill\upperright}\\
\ifthenelse{\isundefined{\middleleft}}{}{\middleleft} &
\ifthenelse{\isundefined{\middlecenter}}{}{\centering\middlecenter} &
\ifthenelse{\isundefined{\middleright}}{}{\hfill\middleright}\\
\ifthenelse{\isundefined{\lowerleft}}{}{\lowerleft} &
\ifthenelse{\isundefined{\lowercenter}}{}{\centering\lowercenter} &
\ifthenelse{\isundefined{\lowerright}}{}{\hfill\lowerright}\\
\end{tabular}
\end{minipage}
}
\end{document}
