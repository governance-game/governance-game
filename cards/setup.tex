\newcommand{\cardname}{1. Setup}
\newcommand{\cardtype}{Rules}
\newcommand{\cardtext}{Get as many friends as you like, as long as they can fit around the same table.
Select or randomly pick one \textbf{scenario} card, read it out loud. Select or randomly pick one \textbf{starting state} card. Place \textbf{actor} cards on the table as the starting state card shows.
Remember to add end users from the \textbf{actor} cards according to the scenario card.}
\newcommand{\cardpicture}{}
\input{rules-template}
